apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app   
spec:
  replicas: 5
  selector:
    matchLabels:
      app: fastapi-app 
  template:
    spec:
      containers:
        - name: fastapi
  