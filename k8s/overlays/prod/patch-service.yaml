apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
  labels:
    app: fastapi-app
spec:
  type: LoadBalancer  
  ports:
  - port: 80         
    targetPort: 8000 
    protocol: TCP
    name: http
  - name: metrics   
    port: 8080
    targetPort: 8080
  selector:
    app: fastapi-app